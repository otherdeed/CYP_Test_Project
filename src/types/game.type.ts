export type Game = {
    title: string,
    genre: string,
    platform: string,
    rating: number,
    cover: string,
    description: string,
}

export type GameData = {
    title: string,
    genre: string,
    platform: string,
    rating: number,
    cover: string,
    description: string,
    id: string
}